name: Test Action

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: 23f2002284@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Verify action ran
        run: echo "Action completed successfully"
